Nuzi Vehicles v1.0.6

- fixes release position saving for movable UI elements
- drag-stop now prefers `GetEffectiveOffset()` before falling back to `GetOffset()`
- improves persistence for the main window, speed HUD, compass window, and toggle button across reloads and relogs
